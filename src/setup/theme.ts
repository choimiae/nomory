import {createTheme} from "@mui/material/styles";

const theme = createTheme({
	palette: {
		primary: {
			main: "#3d6cb3"
		},
		secondary: {
			main: "#111"
		}
	},
	shape: {
		borderRadius: 0,
	},
})

export default theme;