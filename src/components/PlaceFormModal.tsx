import React from "react";

interface PlaceFormType {
	title: string,
	content: string,
	date: string,
	memo: string
}

const PlaceFormModal = (props:PlaceFormType) => {

	const {title, content, date, memo} = props;


	return (
		<></>
	)
}

export default PlaceFormModal;